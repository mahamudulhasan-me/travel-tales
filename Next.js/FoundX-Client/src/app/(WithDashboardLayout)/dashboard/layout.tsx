import { ReactNode } from "react";

const DashboardLayout = ({ children }: { children: ReactNode }) => {
  return <div>this is dashboard layout with {children}</div>;
};

export default DashboardLayout;
